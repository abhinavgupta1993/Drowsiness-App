package com.drowsiness.ai.helper

interface DetectionHelper {

    fun checkYawningCount(yawningCount :Int)
    fun checkEyesCount(eyesCount :Int)
    fun checkDistractionCount(distractionCount : Int)

}